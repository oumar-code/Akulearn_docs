# AkuAI — ML / inference dependencies
# Install in addition to the shared platform requirements.txt
# These are intentionally pinned loosely; tighten to exact versions before production.

# Deep learning runtime
torch>=2.3.0,<3.0.0
torchvision>=0.18.0,<1.0.0

# HuggingFace ecosystem
transformers>=4.41.0,<5.0.0
tokenizers>=0.19.0,<1.0.0
accelerate>=0.30.0,<1.0.0
datasets>=2.19.0,<3.0.0
huggingface-hub>=0.23.0,<1.0.0

# Local LLM runtime (GGUF / llama.cpp bindings for Gemma on Edge Hubs)
llama-cpp-python>=0.2.70,<1.0.0

# Sentence / semantic embeddings
sentence-transformers>=3.0.0,<4.0.0

# Optimised inference utilities
einops>=0.8.0,<1.0.0
safetensors>=0.4.3,<1.0.0
