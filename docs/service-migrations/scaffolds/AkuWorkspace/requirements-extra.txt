# AkuWorkspace — extra runtime dependencies
# Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-extra.txt

# Async HTTP client for upstream service calls (AkuAI, Aku-DaaS, Akudemy)
httpx==0.27.0

# Redis async client for per-user context storage
redis[hiredis]==5.0.4

# Pydantic settings — reads config from .env / environment variables
pydantic-settings==2.2.1
aku-platform-contracts @ git+https://github.com/oumar-code/aku-platform-contracts.git@v0.1.1
