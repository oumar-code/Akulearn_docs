# Aku-DaaS extra dependencies
# Install alongside the base requirements.txt:
#   pip install -r requirements.txt -r requirements-extra.txt

# ── Async HTTP client (IGHub metadata forwarding) ────────────────────────────
httpx[http2]>=0.27.0                   # async HTTP/2 client for IGHub & external service calls

# ── Data processing (anonymisation pipeline) ─────────────────────────────────
pandas>=2.2.0                          # DataFrame-based dataset loading, transformation, and output
pyarrow>=16.0.0                        # Parquet read/write support (pandas backend)

# ── Anonymisation utilities ───────────────────────────────────────────────────
# Uncomment once integrating a real k-anonymity implementation:
# anonympy>=0.3.0                      # k-anonymity, l-diversity, t-closeness helpers
# pycanon>=1.1.0                       # measure k-anonymity / l-diversity metrics on DataFrames

# ── Test data generation ──────────────────────────────────────────────────────
faker>=25.0.0                          # synthetic PII-free dataset generation for integration tests

# ── Storage backends (production replacements for in-memory dicts) ────────────
redis[hiredis]>=5.0.0                  # async Redis for distributed status store & pub/sub
# boto3>=1.34.0                        # AWS S3 / MinIO for raw + anonymised dataset object storage

# ── Validation ────────────────────────────────────────────────────────────────
# pandera>=0.19.0                      # schema validation for pandas DataFrames (pipeline step 1)

# ── Auth (enable when adding JWT gateway auth) ───────────────────────────────
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4
