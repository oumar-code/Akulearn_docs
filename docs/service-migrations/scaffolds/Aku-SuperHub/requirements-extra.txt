# Aku-SuperHub — extra dependencies (beyond the shared base requirements)
# Install with: pip install -r requirements-extra.txt

# HTTP framework
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# Data validation
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# Database / ORM
sqlalchemy[asyncio]>=2.0.30,<3.0.0
asyncpg>=0.29.0,<1.0.0          # PostgreSQL async driver
alembic>=1.13.0,<2.0.0          # Database migrations

# Caching / message bus
redis[hiredis]>=5.0.3,<6.0.0

# Observability
opentelemetry-sdk>=1.24.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.45b0
prometheus-client>=0.20.0,<1.0.0

# Auth (JWT verification)
python-jose[cryptography]>=3.3.0,<4.0.0

# Utilities
httpx>=0.27.0,<1.0.0            # Async HTTP client (inter-service calls)
tenacity>=8.3.0,<9.0.0          # Retry logic for ML pipeline calls
python-dotenv>=1.0.1,<2.0.0
aku-platform-contracts @ git+https://github.com/oumar-code/aku-platform-contracts.git@v0.1.1
