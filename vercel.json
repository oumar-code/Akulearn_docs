{
  "projects": [
    {
      "src": "akulearn-dashboard/package.json",
      "use": "@vercel/next"
    },
    {
      "src": "mkdocs.yml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "site"
      },
      "buildCommand": "pip install -r requirements.txt && mkdocs build"
    }
  ]
}
