name: "Akudemy — Exam Papers Stub"

# ──────────────────────────────────────────────────────────────────────────────
# What this workflow does
# ──────────────────────────────────────────────────────────────────────────────
# Creates a stub PR in oumar-code/Akudemy with:
#   - data/exam_papers/.gitkeep  — placeholder for the exam question dataset
#   - scripts/exam_paper_scraper.py — stub with usage instructions
#   - scripts/README.md            — usage guide for the scraper and dataset
#
# This initialises the exam papers directory in Akudemy without needing any
# local files. Exam question content is generated entirely in CI:
#   - Trigger generate-jss-content-starters.yml (content_tier=tier1_bece) →
#     runs pipeline/bece_scraper.py in CI to generate BECE/WAEC/NECO/JAMB
#     question bundles fresh and PRs them to Aku-Content.
#
# Prerequisites
# ──────────────────────────────────────────────────────────────────────────────
# Create a repo secret named GH_PAT:
#   Settings → Secrets and variables → Actions → New repository secret
#   Name : GH_PAT
#   Value: A classic Personal Access Token with scopes: repo, workflow
#   (Generate at https://github.com/settings/tokens)
#
# How to trigger
# ──────────────────────────────────────────────────────────────────────────────
# Go to: Actions → "Akudemy — Exam Papers Stub" → Run workflow
# Optionally tick "Dry run" to preview without making changes.
# ──────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run — preview steps without pushing anything"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]

jobs:
  stub:
    name: Create exam papers stub in Akudemy
    runs-on: ubuntu-latest

    permissions:
      contents: read  # only reads from this repo; pushes go to Akudemy via PAT

    env:
      GITHUB_ORG: oumar-code
      TARGET_REPO: Akudemy
      BRANCH_NAME: feat/exam-papers-stub
      GH_TOKEN: ${{ secrets.GH_PAT }}
      DRY_RUN: ${{ inputs.dry_run || 'false' }}

    steps:
      # ── 1. Checkout Akulearn_docs ─────────────────────────────────────────────
      - name: Checkout Akulearn_docs
        uses: actions/checkout@v4

      # ── 2. Configure git identity ────────────────────────────────────────────
      - name: Configure git identity
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name  "github-actions[bot]"

      # ── 3. Clone Akudemy via PAT ──────────────────────────────────────────────
      - name: Clone Akudemy
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          REPO_URL="https://x-access-token:${GH_PAT}@github.com/${GITHUB_ORG}/${TARGET_REPO}.git"
          echo "Cloning ${GITHUB_ORG}/${TARGET_REPO}..."
          git clone --depth 1 "${REPO_URL}" "${RUNNER_TEMP}/${TARGET_REPO}"
          git -C "${RUNNER_TEMP}/${TARGET_REPO}" remote set-url origin "${REPO_URL}"

      # ── 4. Create branch and stub files ──────────────────────────────────────
      - name: Create stub files
        run: |
          TARGET_CLONE="${RUNNER_TEMP}/${TARGET_REPO}"

          if [[ "$DRY_RUN" == "true" ]]; then
            echo "🔍 DRY-RUN mode — files that would be written:"
            echo "  [dry-run] data/exam_papers/.gitkeep"
            echo "  [dry-run] scripts/exam_paper_scraper.py (stub)"
            echo "  [dry-run] scripts/README.md"
            exit 0
          fi

          git -C "${TARGET_CLONE}" checkout -b "${BRANCH_NAME}"

          # Delegate file writing to the helper script (avoids YAML/heredoc incompatibility)
          bash "${GITHUB_WORKSPACE}/docs/service-migrations/create-exam-papers-stub-files.sh" "${TARGET_CLONE}"

      # ── 5. Commit and push ────────────────────────────────────────────────────
      - name: Commit and push
        if: inputs.dry_run != 'true'
        run: |
          TARGET_CLONE="${RUNNER_TEMP}/${TARGET_REPO}"
          cd "${TARGET_CLONE}"

          git add .
          if git diff --cached --quiet; then
            echo "ℹ️  Nothing to commit — stub files may already be in place."
            exit 0
          fi

          git commit -m "feat: add exam papers directory structure and scripts placeholder"
          git push -u origin "${BRANCH_NAME}"
          echo "✅ Pushed branch: ${BRANCH_NAME}"

      # ── 6. Open pull request ──────────────────────────────────────────────────
      - name: Open pull request
        if: inputs.dry_run != 'true'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          PR_BODY: |
            Adds the directory structure and scripts placeholder for the exam papers migration.

            ## What's included
            - `data/exam_papers/.gitkeep` — placeholder for the exam question dataset (WAEC/NECO/JAMB/BECE)
            - `scripts/exam_paper_scraper.py` — stub with usage instructions
            - `scripts/README.md` — usage guide for the scraper and dataset

            ## How to populate exam questions (CI — no local machine needed)
            Trigger `generate-jss-content-starters.yml` with `content_tier=tier1_bece` →
            runs `pipeline/bece_scraper.py --all` in CI and PRs the generated question bundles
            to `oumar-code/Aku-Content`.

            _Automated by the stub-akudemy-exam-papers GitHub Actions workflow._
            Migration tracker: https://github.com/oumar-code/Akulearn_docs/blob/main/docs/ecosystem-map.md
        run: |
          TARGET_CLONE="${RUNNER_TEMP}/${TARGET_REPO}"
          if ! git -C "${TARGET_CLONE}" ls-remote --exit-code origin "${BRANCH_NAME}" &>/dev/null; then
            echo "ℹ️  Branch not pushed (nothing to commit) — skipping PR creation."
            exit 0
          fi

          gh pr create \
            --repo "${GITHUB_ORG}/${TARGET_REPO}" \
            --base main \
            --head "${BRANCH_NAME}" \
            --title "feat: add exam papers directory structure and scraper placeholder" \
            --body "${PR_BODY}" \
            || echo "ℹ️  PR may already exist for this branch."

      # ── 7. Summary ────────────────────────────────────────────────────────────
      - name: Summary
        if: always()
        run: |
          echo "════════════════════════════════════════════════════"
          echo "  Workflow complete"
          echo "  Target repo : ${GITHUB_ORG}/${TARGET_REPO}"
          echo "  Branch      : ${BRANCH_NAME}"
          echo "  Dry run     : ${DRY_RUN}"
          echo "════════════════════════════════════════════════════"
          if [[ "$DRY_RUN" != "true" ]]; then
            echo "  Next steps:"
            echo "  1. Review and merge the stub PR in Akudemy"
            echo "  2. Trigger generate-jss-content-starters.yml (content_tier=tier1_bece) to generate exam questions in CI"
            echo "  3. Wire data/exam_papers/ into Akudemy content API (routers/content.py)"
          fi
